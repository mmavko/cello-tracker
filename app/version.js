// App version, shown in the Home footer + test panel so a stale page is obvious.
// `deploy.sh` overwrites this with a build stamp at deploy time; "dev" means
// you're running the un-stamped source (e.g. a local server).
export const VERSION = "dev";
