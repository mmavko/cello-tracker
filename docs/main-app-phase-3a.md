# Phase 3a — Maintenance: lesson-minutes retrofit + Detector rename

**Done — tombstoned.** Per-lesson minutes (`lessonDays[]` → `[{date, lenMin}]`,
`config.lessonLenMin` removed) and the `settings` → `Detector` page rename, both
landed with `npm test` green. Full history: [main-app-implementation.md](main-app-implementation.md)
Phase 3a row + chronicles + git.
